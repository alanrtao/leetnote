# Unordered map
- Unordered map == defaultdict as access of nonexistent value creates a default first

# Iterators
- Reverse iterators also use `++`. For example `++vec.rbegin()` will give the second last element.